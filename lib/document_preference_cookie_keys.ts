/** Cookie storing the theme mode preference (`light` | `dark` | `system`). */
export const THEME_MODE_COOKIE_KEY = "stt-theme-mode";

/** Cookie storing the resolved theme applied to the document. */
export const THEME_COOKIE_KEY = "stt-theme";

/** Cookie storing the active color palette id. */
export const COLOR_PALETTE_COOKIE_KEY = "stt-palette";

/** Cookie storing whether compact lists are enabled (`true` | `false`). */
export const COMPACT_LISTS_COOKIE_KEY = "stt-compact-lists";
